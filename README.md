# arduino_usb_key
Arduino with SD shield used as a USB key over Serial

# Setup

Add a SD shield to an arduino, change the chipSelect value in arduino_code.ino.

# Run

Plug the Arduino on your target and copy the PowerShell script in order to save files on your SD card.
